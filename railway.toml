[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
dockerBuildArgs = ["NEXT_PUBLIC_DFLOW_METADATA_URL", "NEXT_PUBLIC_DFLOW_TRADE_URL", "NEXT_PUBLIC_SOLANA_RPC", "NEXT_PUBLIC_SOLANA_RPC_URL"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
